# Конфигурация Krakend API Gateway

Этот файл конфигурации описывает настройки для Krakend API Gateway. Ниже приведено объяснение каждой строки.

```json
{
"$schema": "https://www.krakend.io/schema/v2.5/krakend.json",
// **Указывает на схему JSON**, которая используется для валидации конфигурационного файла. 
// Это помогает убедиться, что конфигурация соответствует ожидаемому формату.

"version": 3,
// **Версия конфигурации**. Указывает, какая версия схемы используется в этом файле.

"name": "My lovely gateway",
// **Имя вашего API Gateway**. Это может быть любое строковое значение, которое помогает идентифицировать ваш шлюз.

"port": 8080,
// **Порт, на котором будет запущен ваш API Gateway**. В данном случае это порт 8080.

"cache_ttl": "3600s",
// **Время жизни кэша (TTL) в секундах**. Указывает, как долго кэшированные данные будут храниться перед их обновлением. 
// Здесь указано 3600 секунд (1 час).

"timeout": "3s",
// **Время ожидания (timeout) для запросов к бэкенду**. Если ответ не будет получен в течение 3 секунд, запрос будет прерван.

"extra_config": {
// **Дополнительные настройки для Krakend**, которые могут включать в себя различные конфигурации.

"telemetry/logging": {
    // **Настройки для логирования**.

    "level": "DEBUG",
    // **Уровень логирования**. В данном случае установлен на DEBUG, что позволяет получать более подробные сообщения о работе шлюза.

    "prefix": "[KRAKEND]",
    // **Префикс для логов**. Используется для идентификации сообщений, относящихся к Krakend.

    "syslog": false,
    // **Указывает, следует ли отправлять логи в syslog**. Здесь установлено значение false, что означает, что логи не будут отправляться в syslog.

    "stdout": true
    // **Указывает, следует ли выводить логи в стандартный вывод (stdout)**. Здесь установлено значение true.
},

"telemetry/metrics": {
    // **Настройки для сбора метрик**.

    "collection_time": "60s",
    // **Время сбора метрик**. Указывает, как часто метрики будут собираться (каждые 60 секунд).

    "proxy_disabled": false,
    // **Указывает, отключен ли прокси для сбора метрик**. Здесь установлено значение false, что означает, что прокси включен.

    "router_disabled": false,
    // **Указывает, отключен ли маршрутизатор для сбора метрик**. Здесь установлено значение false, что означает, что маршрутизатор включен.

    "backend_disabled": false,
    // **Указывает, отключены ли бэкенды для сбора метрик**. Здесь установлено значение false, что означает, что бэкенды включены.

    "endpoint_disabled": false,
    // **Указывает, отключены ли конечные точки для сбора метрик**. Здесь установлено значение false, что означает, что конечные точки включены.

    "listen_address": ":8090"
    // **Адрес, на котором будут слушаться метрики**. В данном случае это порт 8090.
},

"security/cors": {
    // **Настройки CORS (Cross-Origin Resource Sharing)**.

    "allow_origins": [ "http://192.168.99.100:3000", "http://localhost:3000" ],
    // **Список разрешенных источников (origins)**, которые могут делать запросы к вашему API. 
    // Здесь разрешены запросы с указанных адресов.

    "allow_methods": [ "POST", "GET" ],
    // **Список разрешенных HTTP методов**. В данном случае разрешены методы POST и GET.

    "allow_headers": [ "Origin", "Authorization", "Content-Type" ],
    // **Список разрешенных заголовков**, которые могут быть отправлены в запросах. 
    // Здесь разрешены заголовки Origin, Authorization и Content-Type.

    "expose_headers": [ "Content-Length" ],
    // **Список заголовков, которые могут быть доступны клиенту**. 
    // Здесь доступен заголовок Content-Length.

    "max_age": "12h"
    // **Максимальное время (время
